# Expo environment variables (prefix must be EXPO_PUBLIC_)
# Optional override for the backend URL. If unset, src/constants/config.js
# falls back to its built-in host selection (10.0.2.2 for Android emulator,
# localhost otherwise).
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.100:5000/api
