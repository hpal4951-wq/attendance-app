/**
 * Color constants — canonical source is src/theme.js.
 * This file re-exports them so components can import from constants/colors
 * without depending on the theme path directly.
 */
export {
  COLORS,
  SPACING,
  FONT_SIZE,
  SHADOW,
  RADIUS,
} from "../theme";
