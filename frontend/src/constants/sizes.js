/**
 * Size constants — re-exports from theme plus additional layout helpers.
 */
export { SPACING, FONT_SIZE, RADIUS, SHADOW } from "../theme";

import { SPACING } from "../theme";

export const ICON_SIZE = {
  xs: 14,
  sm: 18,
  md: 22,
  lg: 28,
  xl: 36,
};

export const BUTTON_HEIGHT = {
  sm: 40,
  md: 50,
  lg: 56,
};

export const INPUT_HEIGHT = {
  sm: 40,
  md: 48,
  lg: 54,
};
